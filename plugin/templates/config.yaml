schema: 1
project: my-project

commands:
  test: ""
  lint: ""
  typecheck: ""
  build: ""

documentation:
  instructions: CLAUDE.md
  state: .protocol/STATE.md
  decisions: .protocol/DECISIONS.md
  # existing_adr: docs/adr   # uncomment if you have existing ADR files

navigation:
  provider: auto   # auto | codebase-memory | rg

risk:
  # ask = the PreToolUse hook escalates matching tool calls to you for approval.
  # off = no enforcement; the categories below are then only advisory to the model.
  enforcement: ask
  require_approval:
    authentication: true
    authorization: true
    payments: true
    data_migration: true
    public_api: true
    production: true
    destructive_operations: true
    secrets_and_credentials: true
